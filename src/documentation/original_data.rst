.. _original_data:

*************
Original data
*************

Documentation of the different datasets in *original_data*.

In the original data section we stored the raw data, which we should not manipulate to ensure reproducibility.

If we want to include multiple data sets, we can also create subfolders for the sake of a clear structure.
